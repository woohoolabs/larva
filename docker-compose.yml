version: '2'

services:
    larva-php:
        image: woohoolabs/php:3.2
        container_name: larva-php
        volumes:
            - .:/code
        command: /bin/bash -c "cd /code; php vendor/bin/phpunit"
